# Atlas device configuration
# Copy this file to .env.local and fill in the values.
# Never commit .env.local to version control.

# Required — issued by the ai.gency admin panel
ATLAS_DEVICE_KEY=

# Required — base URL of the ai.gency platform (no trailing slash)
BACKEND_URL=

# Optional — display name for the assistant
ATLAS_USER_NAME=
